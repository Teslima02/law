/*
 * Atendee Messages
 *
 * This contains all the text for the Atendee container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Atendee';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Atendee container!',
  },
});
